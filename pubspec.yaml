name: quizz_app
description: "Aplikasi Kuis Pilihan Ganda Multi-Subject untuk SMA"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.0.5
  
  # UI & Fonts
  google_fonts: ^6.1.0
  iconsax: ^0.0.8
  
  # Animations
  lottie: ^3.0.0
  
  # Material Icons
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
